# POE2Helper Update Server

Private update server for POE2Helper. Do not share.

## Files
- `version.json` — app checks this for updates
- `offsets.dat` — memory offsets for game patches  
- `api/health.js` — Cloudflare Worker health endpoint
- `api/health.php` — PHP health endpoint (alternative)
